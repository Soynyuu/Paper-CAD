# Demo mode environment variables
# Production-like performance on localhost

# Environment mode (demo = production settings with localhost support)
ENV=demo

# Frontend URL (localhost)
FRONTEND_URL=http://localhost:8080

# CORS settings (false for demo mode, use explicit origins)
CORS_ALLOW_ALL=false

# Server port
PORT=8001

# Worker count (production-like setting)
WORKERS=2
