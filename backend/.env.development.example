# Backend Development Environment Configuration

# Environment mode (development, demo, production)
ENV=development

# CORS Configuration
CORS_ALLOW_ALL=true
FRONTEND_URL=http://localhost:8080

# CityGML Cache Configuration (Optional, for Tokyo 23 wards)
# Set to true to enable pre-downloaded CityGML cache for 10-50x faster processing
# Requires running setup script first: python scripts/setup_citygml_cache.py
CITYGML_CACHE_ENABLED=false
CITYGML_CACHE_DIR=data/citygml_cache

# SVG Upload Limits (optional)
# SVG_MAX_FILES=100
# SVG_MAX_FILE_SIZE_MB=50
# SVG_MAX_TOTAL_SIZE_MB=200
