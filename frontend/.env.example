# Backend API URL for Step Unfold service
# Default (if unset): https://backend-paper-cad.soynyuu.com/api
# Development example:
STEP_UNFOLD_API_URL=http://localhost:8001/api

# (Optional) WebSocket URL for live preview, if used in the future
# Development example:
STEP_UNFOLD_WS_URL=ws://localhost:8001/ws/preview

# (Optional) Cesium terrain asset ID for PLATEAU terrain (Cesium ion)
# Default: 3258112
# Example:
CESIUM_TERRAIN_ASSET_ID=3258112

# (Optional) Cesium ion access token for PLATEAU terrain asset
# Example:
CESIUM_TERRAIN_ION_TOKEN=your_terrain_token

# (Optional) Preferred LOD for Cesium picker 3D Tiles (1-3, default: 3)
CESIUM_PICK_LOD=3

# Notes:
# - You can create environment-specific files like `.env.production` or `.env.development`.
# - The build will load `.env.<NODE_ENV>` first, then `.env` as fallback.
